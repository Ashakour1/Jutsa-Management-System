{
  "version": 2,
  "builds": [
    {
      "src": "./server.js",
      "use": "@vercel/node",
      "config": {
        "buildCommand": "npm run build"
      }
    }
  ],
  "routes": [
    { "src": "/(*)", "dest": "/server.js" },
    { "src": "/api/competitors(.*)", "dest": "/server.js" }
  ]
}
